import Workspace from "@/components/ai/Workspace";

export const metadata = {
  title: "Zenthra AI — Ask a question. Zenthra does the research.",
};

export default function AIPage() {
  return <Workspace />;
}
